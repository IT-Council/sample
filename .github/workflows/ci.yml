name: My first workflow # A name for workflow

on:              # when to run the workflows
  push:          # on push events 
    branches:    # specify branches
        - main    # Run on pushes to the 'main' branch 

jobs:
 build:
  runs-on: ubuntu-latest 

  steps:
   - name: checkout code 
     uses: actions/checkout@v3

  - name: say hello 
    run: echo "Hello, Devsecops Team!"
